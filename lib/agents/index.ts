/**
 * agents/index.ts – Barrel export for AI agent modules.
 *
 * Each agent is responsible for one step of the redesign pipeline.
 * Phase 1 only exports placeholder stubs so imports resolve.
 *
 * TODO (Phase 2+):
 *   - openai-agent.ts  → OpenAI chat/code generation
 *   - gemini-agent.ts  → Google Gemini helper via fetch
 *   - vision-agent.ts  → Screenshot analysis with Cloud Vision
 */

export {};
